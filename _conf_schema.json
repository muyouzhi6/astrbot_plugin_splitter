{
  "split_regex": {
    "description": "分段正则表达式。匹配到的字符将被保留在上一段的末尾。例如使用 `[。？！\\n]+` 可以按句号、感叹号、问号或换行符分段。",
    "type": "string",
    "default": "[。？！\\n]+"
  },
  "delay": {
    "description": "分段发送间隔(秒)",
    "type": "float",
    "default": 1.5
  }
}
